name: ass_downloader_example
description: Assets Downloader Example
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0


dev_dependencies:
  flutter_lints: ^3.0.0
  very_good_analysis: ^5.1.0

flutter:
  assets:
    - .env
